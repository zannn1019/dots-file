# Core
requests>=2.28

# Wallpaper analysis (Ollama-based tagging + color extraction)
Pillow>=9.0

# Lyrics fetching for music player widget
syncedlyrics>=1.0

# Password authentication (optional — only one needed)
# python-pam>=2.0
# pamela>=1.0
